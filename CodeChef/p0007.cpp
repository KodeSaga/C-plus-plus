//[inComplete]Problem statement - https://www.codechef.com/PLUM2019/problems/VISION

#include <iostream>
#include <string>
using namespace std;

int main() {
    string brain,body;
	cin >> brain >> body;
	int brain_len = brain.length();
	int body_len = body.length();
	int length,k;
	
	if (brain_len >= body_len)
	    length = brain_len;
	else
	    length = body_len;
	    
	char brain_wire[length+1], body_wire[length+1];
	for(int i=0;i<=length;i++){
	    k=i;
	    if()
	}
	
	    
	return 0;
}
